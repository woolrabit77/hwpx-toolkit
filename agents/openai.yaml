interface:
  display_name: "Standalone HWPX Document Automation"
  short_description: "Create and validate Korean HWPX documents offline."
  default_prompt: "Use $hwp-hwpx-document-automation to plan, confirm, build, and validate a minimal untemplated HWPX document; use a preset only when I explicitly request one."
