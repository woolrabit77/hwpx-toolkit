display_name: Standalone HWPX Document Automation
short_description: Create and validate Korean HWPX files without Hancom or internet access.
default_prompt: Convert the request into a compact Document Spec, select an approved template when appropriate, build the HWPX file with the single CLI, and run full validation.
